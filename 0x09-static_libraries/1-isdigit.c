#include <stdio.h>
/**
  * _isdigit - checks if the character is a digitt 0 to 9
  * @c: character to be checked
  * Return: 1 if digit, otherwise 0
  */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
