#include "main.h"
/**
 * add - addss two integers and return the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: sum of and b
 */
int add(int a, int b)
{
int c;
c = a + b;
return (c);
}
